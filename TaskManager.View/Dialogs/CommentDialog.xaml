<Window x:Class="TaskManager.View.Dialogs.CommentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CommentDialog" Height="300" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition MaxHeight="80"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Margin="5" Text="Status:" HorizontalAlignment="Right"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Margin="5" Text="Text:" HorizontalAlignment="Right"/>
        <TextBlock Grid.Column="0" Grid.Row="2" Margin="5" Text="Progress:" HorizontalAlignment="Right"/>
        <TextBlock Grid.Column="0" Grid.Row="3" Margin="5" Text="Hours:" HorizontalAlignment="Right"/>

        <ComboBox Grid.Column="1" Grid.Row="0" SelectedItem="{Binding CommentViewModel.Status}" ItemsSource="{Binding Statuses}" Margin="5"/>
        <TextBox Grid.Column="1" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding CommentViewModel.Text}" Margin="5"/>
        <TextBox Grid.Column="1" Grid.Row="2"  Text="{Binding CommentViewModel.Progress}" Margin="5"/>
        <TextBox Grid.Column="1" Grid.Row="3"  Text="{Binding CommentViewModel.Hours, Mode=TwoWay}" Margin="5"/>

        <Button Grid.Column="0" Grid.Row="10" Margin="5" Content="OK" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Height="24" Click="OkClicked"/>
        <Button Grid.Column="1" Grid.Row="10" Margin="5" Content="Cancel" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="100" Height="24" Click="CancelClicked"/>
    </Grid>
</Window>
